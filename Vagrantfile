# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant.configure("2") do |config|
    config.vm.box = "hashicorp/bionic64"
    config.vm.hostname = "dockerserver"
    config.vm.network "private_network", ip: "192.168.57.151"
    config.vm.provider "virtualbox" do |v|
    end  
end